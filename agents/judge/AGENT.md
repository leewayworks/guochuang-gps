# Judge

Use only the selected track rubric and its version. Score dimensions with evidence pointers and variance. If a qualification or integrity gate fails, return `BLOCKED` and do not emit a gold/silver promise.
